[package]
edition = "2018"
name = "hc_lib_ranking_index"
version = "0.3.0"

[lib]
crate-type = ["cdylib", "rlib"]
name = "hc_lib_ranking_index"

[dependencies]
serde = "1"
hdk = {version = "0.1.0-beta-rc.1", features = ["encoding"]}
